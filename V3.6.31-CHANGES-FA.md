# تغییرات نسخه 3.6.31

این نسخه بر پایه `v3.6.30` ساخته شده و روی نمایش دقیق اتصال کاربران، به‌روزرسانی سابسکریپشن، روت اختصاصی Streisand و مدیریت سریع‌تر کلاینت‌ها تمرکز دارد.

## تغییرات اصلی

- حفظ وضعیت آنلاین کاربر از API اتصال Xray حتی زمانی که اتصال برقرار است ولی ترافیکی ردوبدل نمی‌شود.
- حفظ نسبت دقیق کاربر و اینباند در تمام عمر اتصال؛ لاگ یک‌باره‌ی اتصال دیگر پس از پنجره کوتاه از رابط حذف نمی‌شود.
- نمایش «اینباند متصل» در جدول و کارت موبایل کلاینت‌ها، جدا از فهرست اینباندهای منتسب به حساب.
- افزودن Pin چندتایی و دائمی برای کلاینت‌ها با ذخیره در دیتابیس و مرتب‌سازی سمت سرور.
- رعایت کامل فیلترها برای Pin: کلاینت سنجاق‌شده در فیلتر منقضی/غیرفعال/کم‌حجم فقط در صورت تطابق واقعی نمایش داده می‌شود.
- ارسال هدر استاندارد `Profile-Update-Interval` همراه با هدرهای ضدکش و قابل‌خواندن برای کلاینت‌های وب.
- افزودن تنظیم مستقل Streisand برای فعال/غیرفعال کردن و ویرایش لینک روت؛ این لینک وارد بدنه ساب سایر برنامه‌ها نمی‌شود.
- قرار دادن روت Streisand در منوی iOS صفحه سابسکریپشن و QR کلاینت با لینک پیش‌فرض درخواستی.
- اصلاح تولید خودکار TypeScript/Zod برای نوع `ClientReportInbound`.

## نکته سازگاری برنامه‌ها

مقدار فاصله به‌روزرسانی از سمت سرور به‌صورت ساعت و طبق استاندارد ارسال می‌شود. اجرای خودکار آن در اختیار برنامه کلاینت است؛ برنامه‌هایی که `Profile-Update-Interval` را پشتیبانی نمی‌کنند باید از تنظیم داخلی Auto Update استفاده کنند. در Streisand می‌توان گزینه Update on Open را نیز فعال کرد.

## نتیجه اعتبارسنجی

- `go test ./...` — موفق
- `go vet ./...` — موفق
- تست Race برای Xray و سرویس‌های وب — موفق
- TypeScript typecheck و ESLint — موفق
- ۱۰۴۶ تست Frontend (unit + components) — موفق
- Build کامل Frontend، تولید OpenAPI و Build باینری — موفق

تست Storybook به نصب جداگانه Chromium مربوط به Playwright نیاز دارد و جزو ۱۰۴۶ تست unit/components بالا نیست.
