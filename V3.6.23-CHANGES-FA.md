# تغییرات Fara Xray v3.6.23

این نسخه مستقیماً بر پایه سورس سفارشی v3.6.22 ساخته شده و تغییرات اختصاصی نسخه‌های قبلی فورک حفظ شده‌اند.

## DNS قابل تنظیم برای اشتراک Xray JSON

- یک گزینه جدید با عنوان **DNS اشتراک Xray JSON** به صفحه **تنظیمات Xray** اضافه شد.
- مقدار پیش‌فرض DNS اشتراک‌های JSON از `8.8.8.8` به `1.1.1.1` تغییر کرد.
- گزینه‌های آماده زیر در رابط کاربری قرار گرفتند:
  - Cloudflare: `1.1.1.1`
  - Cloudflare DoH: `https://1.1.1.1/dns-query`
  - Google: `8.8.8.8`
  - Quad9: `9.9.9.9`
- امکان وارد کردن مقدار سفارشی سازگار با Xray نیز وجود دارد.
- مقدار انتخاب‌شده در جدول عمومی Settings ذخیره می‌شود؛ بنابراین هیچ migration یا تغییر schema دیتابیس لازم نیست.
- تغییر DNS روی درخواست بعدی اشتراک اعمال می‌شود و برای تغییر DNS به restart پنل یا Xray نیاز نیست.
- ساخت کانفیگ JSON به‌صورت request-scoped انجام می‌شود تا تغییر DNS، template مشترک را mutate نکند و درخواست‌های هم‌زمان روی هم اثر نگذارند.

## نکته مهم درباره Shadowsocks

گزینه جدید روی **اشتراک Xray JSON** اثر دارد. استاندارد لینک خام Shadowsocks (`ss://` / SIP002) فیلد استانداردی برای اجبار DNS کلاینت ندارد؛ بنابراین DNS برنامه‌هایی که فقط لینک خام `ss://` را import می‌کنند همچنان باید در خود برنامه تنظیم شود.

## سازگاری و ایمنی تغییرات

- منطق inboundها، کاربران، ترافیک، گزارش‌ها، مقصدها، WARP/NordVPN و تنظیمات اختصاصی v3.6.22 دست‌نخورده باقی مانده است.
- endpoint موجود تنظیمات Xray توسعه داده شده و endpoint جدیدی که مسیرهای قبلی را بشکند اضافه نشده است.
- اگر کلید `subJsonDns` در دیتابیس وجود نداشته باشد، مقدار پیش‌فرض `1.1.1.1` بدون نیاز به migration استفاده می‌شود.
- ورودی DNS trim می‌شود، ورودی خالی به مقدار پیش‌فرض برمی‌گردد، ورودی بیش از 512 کاراکتر و کاراکترهای کنترلی خطی رد می‌شوند.

## نسخه

- `internal/config/version`: `3.6.23`
- نسخه OpenAPI: `3.6.23`
- User-Agent به‌روزرسان مقصدها: `Fara-Xray/3.6.23 destination-prefix-updater`
