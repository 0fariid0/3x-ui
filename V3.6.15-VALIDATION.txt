Fara Xray v3.6.15 validation summary

Base:
- Full Fara Xray v3.6.14 source package.

Root cause fixed:
- ClientsPage imported useSearchParams from react-router-dom, but react-router-dom is not present in frontend/package.json or package-lock.json.
- The import now uses the installed react-router package.

Validated locally:
- Version metadata is 3.6.15.
- All Go files parse and are gofmt-clean.
- All TypeScript/TSX files pass isolated TypeScript transpilation syntax checks.
- Runtime bare-module scan no longer finds react-router-dom.
- Strict JSON files parse successfully.
- GitHub Actions YAML parses successfully.
- Shell scripts pass bash -n.
- Modified files pass whitespace checks.

Release pipeline:
- One Linux amd64 production build.
- npm ci, TypeScript type-check and Vite production build run before Go compilation.
- Static CGO Go build with musl.
- Release archive and SHA-256 are verified before publication.
