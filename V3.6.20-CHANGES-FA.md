# تغییرات Fara Xray v3.6.20

این نسخه مستقیماً از سورس **v3.6.13** ساخته شده است و تغییرات نسخه‌های v3.6.14 تا v3.6.19 وارد آن نشده‌اند.

- باگ خالی‌شدن مقصدها بلافاصله پس از آفلاین‌شدن کاربر برطرف شد.
- مقصدها به‌صورت باکت‌های یک‌دقیقه‌ای و فقط تا ۱۰ دقیقه نگهداری می‌شوند؛ گزارش همیشه جدیدترین بازه واقعی ۵ دقیقه‌ای را نشان می‌دهد.
- اگر کاربر قطع شود، آخرین نمای ۵ دقیقه‌ای تا رسیدن تازه‌ترین رکورد به مرز حذف ۱۰ دقیقه‌ای همچنان قابل مشاهده می‌ماند.
- نقطه سبز فقط زمانی نمایش داده می‌شود که هم خود کاربر واقعاً آنلاین باشد و هم آن مقصد در ۵ دقیقه اخیر دیده شده باشد.
- تازه‌سازی زنده تب مقصدها هر ۱۵ ثانیه حفظ شده است.
- دکمه مستقیم «آمار» به کارت موبایل هر کاربر اضافه و گزینه تکراری آن از منوی سه‌نقطه حذف شد.
- بخش «گزارش دقیق» به‌طور کامل از «اطلاعات کلاینت» حذف شد؛ گزارش فقط از پنجره اختصاصی آمار دریافت و نمایش داده می‌شود.
- بازه پیش‌فرض پنجره آمار، هشدارهای مصرف و APIهای گزارش روی ۲۴ ساعت گذشته قرار گرفت.
- رنگ اصلی قالب برای خط دانلود و رنگ فرعی برای خط آپلود استفاده می‌شود.
- دکمه مداد کنار نام کلاینت در بالای پنجره آمار اضافه شد و همان فرم اصلی ویرایش کلاینت را باز می‌کند.
- مداد ویرایش در آمار صفحه داشبورد نیز فعال است و با انتقال امن به صفحه کلاینت‌ها، فرم همان کاربر را مستقیماً باز می‌کند.
- نسخه OpenAPI و نسخه داخلی پنل به `3.6.20` ارتقا یافت.
