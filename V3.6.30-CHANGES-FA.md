# تغییرات نسخه 3.6.30

این نسخه بر پایه `v3.6.29` ساخته شده و روی رفع باگ‌های انتشار، پایداری تنظیمات، مهاجرت داده و سلامت Build تمرکز دارد.

## اصلاحات اصلی

- فعال شدن Workflow ساخت نسخه توسعه پس از هر Push به شاخه `main`؛ شرط قبلی فقط تگ‌ها را دریافت می‌کرد و `dev-latest` عملاً ساخته نمی‌شد.
- اصلاح مسیر خواندن نسخه در Vite و همسان‌سازی نسخه پنل و OpenAPI روی `3.6.30`.
- رفع Race در تازه‌سازی تنظیمات Xray؛ داده‌های سمت سرور حتی هنگام ویرایش محلی به‌روز می‌شوند، بدون بازنویسی JSON ویرایش‌شده کاربر.
- جلوگیری از ثبت مقدار مصنوعی هنگام پاک کردن فیلدهای عددی زمان‌بندی Restart و Health Monitor.
- اضافه شدن دو جدول مقصدها به مهاجرت SQLite به PostgreSQL تا اطلاعات گزارش مصرف در مهاجرت حذف نشود.
- پذیرش تنظیمات قدیمی که فیلدهای جدید تشخیص مصرف غیرعادی را ارسال نمی‌کنند؛ مقادیر امن در `CheckValid` اعمال می‌شوند.
- حفظ Port نوشته‌شده در `host:port` هنگام وارد کردن Hostهای Node، در صورتی که Port گروه صفر باشد.
- اصلاح تست چرخه Restart برای مدیریت thread-safe پردازش Xray.
- تکمیل مستندات شش Route جدید و همگام‌سازی OpenAPI.
- هماهنگ‌سازی کلیدهای ترجمه در همه ۱۳ زبان و حذف کلیدهای بلااستفاده.
- رفع خطاهای TypeScript، ESLint و تست‌های قدیمی ناسازگار با API فعلی.

## نتیجه اعتبارسنجی

- `go test ./...` — موفق
- `go vet ./...` — موفق
- تست Race برای Xray، MTProto، WebSocket و سرویس‌های وب — موفق
- TypeScript typecheck و ESLint — موفق
- ۱۰۴۱ تست Frontend (unit + components) — موفق
- Build کامل Frontend و Build باینری Linux amd64 — موفق
- اعتبارسنجی Workflow با Actionlint — موفق
- بررسی syntax اسکریپت‌های Shell — موفق

تست Storybook به نصب جداگانه Chromium مربوط به Playwright نیاز دارد و جزو ۱۰۴۱ تست unit/components بالا نیست.
