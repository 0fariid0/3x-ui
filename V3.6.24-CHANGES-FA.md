# تغییرات Fara Xray v3.6.24

## بروزرسانی زنده بخش آمار کاربران

- پنجره «آمار / جزئیات مصرف» کاربر از این نسخه تا زمانی که باز است، هر ۵ ثانیه به‌صورت خودکار بروزرسانی می‌شود.
- اولین دریافت اطلاعات بلافاصله هنگام باز شدن پنجره انجام می‌شود.
- بروزرسانی‌های بعدی در پس‌زمینه و بدون نمایش مجدد Spinner انجام می‌شوند تا رابط کاربری پرش نداشته باشد.
- تمام اطلاعات گزارش کاربر با هر بروزرسانی تازه می‌شوند؛ شامل مصرف، نمودار، آی‌پی‌ها، برنامه‌ها، میزبان‌ها، مقصدها، رخدادها و ناهنجاری‌ها.
- روش polling از timeout زنجیره‌ای استفاده می‌کند تا در اتصال‌های کند چند درخواست همزمان روی هم جمع نشوند.

## نسخه

- `internal/config/version`: `3.6.24`
- نسخه OpenAPI: `3.6.24`
- User-Agent بروزرسان مقصدها: `Fara-Xray/3.6.24 destination-prefix-updater`
