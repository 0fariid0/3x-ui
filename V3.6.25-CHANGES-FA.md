# تغییرات Fara Xray v3.6.25

- وضعیت آنلاین کاربران چند-اینباندی دقیق شد: آنلاین بودن هر کاربر فقط روی همان این‌باندی نمایش داده می‌شود که اتصال همان کاربر روی آن دیده شده است.
- برای جلوگیری از خطای قبلی، دیگر مجموعه «کاربران آنلاین» با مجموعه «اینباندهای دارای ترافیک» به‌صورت عمومی ترکیب نمی‌شود؛ نسبت ایمیل کاربر و تگ این‌باند از همان رکورد اتصال Xray استخراج می‌شود.
- اطلاعات دقیق اتصال در ساختار `panelGuid -> email -> inbound tags` بین نودها نیز منتقل می‌شود. نودهای قدیمی که این API را ندارند همچنان با روش قدیمی نمایش داده می‌شوند.
- در «جزئیات مصرف کاربر»، بخش Hostهای مرتبط با «این‌باندهای متصل» جایگزین شد. بنابراین اگر برای یک این‌باند چند Host/IP تعریف شده باشد، همه آن‌ها آنلاین نمایش داده نمی‌شوند و فقط خود این‌باند واقعاً متصل نمایش داده می‌شود.
- اگر Access Log مدیر غیرفعال باشد، پنل در Runtime یک لاگ موقت و محدود برای تشخیص دقیق این‌باند می‌سازد؛ این لاگ هر ۵ ثانیه پردازش و پس از خواندن پاک می‌شود و اطلاعات خام آن برای گزارش این قابلیت ذخیره نمی‌شود.
- نسخه پنل و OpenAPI به `3.6.25` ارتقا یافت.
