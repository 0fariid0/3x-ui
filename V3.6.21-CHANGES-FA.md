# تغییرات Fara Xray v3.6.21

## رفع خطای بیلد

- خطای Go در فایل `internal/web/service/client_destinations.go` برطرف شد.
- متغیر محلی بلااستفاده `now` که در خط گزارش‌شده توسط GitHub Actions تعریف شده بود، حذف شد.
- هیچ بخش دیگری از منطق ثبت، نگهداری یا نمایش مقصدها برای این رفع اشکال تغییر نکرد.

## قابلیت‌های حفظ‌شده از v3.6.20

- نمایش آخرین بازه واقعی پنج‌دقیقه‌ای مقصدها تا زمان پایان نگهداری ده‌دقیقه‌ای.
- وضعیت سبز بر پایه آنلاین‌بودن کلاینت و مشاهده مقصد در پنج دقیقه اخیر.
- دکمه مستقیم آمار در حالت موبایل.
- حذف گزارش دقیق از پنجره اطلاعات کلاینت و نمایش آن فقط در قسمت آمار.
- پیش‌فرض ۲۴ ساعت برای گزارش‌ها و هشدارهای مصرف.
- رنگ اصلی قالب برای دانلود و رنگ کم‌رنگ‌تر برای آپلود.
- میانبر مداد ویرایش کلاینت در سربرگ پنجره آمار.

## نسخه

- نسخه داخلی پنل، OpenAPI و User-Agent به `3.6.21` ارتقا یافت.
