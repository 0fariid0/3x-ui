# تغییرات Fara Xray v3.6.7

این نسخه یک Hotfix مستقیم برای نسخه 3.6.6 است و قابلیت‌های نسخه 3.6.6 را بدون حذف یا تغییر ظاهری حفظ می‌کند.

## رفع خطای Build بک‌اند

در تابع زیر:

`ClientInsightService.GetReportForRange`

متغیر منطقه زمانی پنل با نام `loc` بعد از اولین استفاده تعریف شده بود. این موضوع خطای زیر را در تمام Buildهای معماری ایجاد می‌کرد:

`internal/web/service/client_insights.go:434:37: undefined: loc`

در نسخه 3.6.7، `loc` قبل از فراخوانی `ensureInsightRollups` مقداردهی می‌شود.

## رفع خطای CI / codegen

فایل تولیدکننده OpenAPI قبلاً در صورت نبود متغیر محیطی، نسخه را به `3.x` تغییر می‌داد؛ درحالی‌که فایل Commit‌شده نسخه `3.6.6` داشت. بنابراین اجرای `npm run gen` باعث ایجاد Diff و شکست Job مربوط به codegen می‌شد.

اکنون تولیدکننده OpenAPI نسخه را مستقیماً از فایل زیر می‌خواند:

`internal/config/version`

به همین دلیل فایل تولیدشده و سورس نسخه همیشه با هم هماهنگ می‌مانند.

## پایه نسخه

- پایه کامل: Fara Xray v3.6.6
- نسخه خروجی: Fara Xray v3.6.7
- تمام قابلیت‌های گزارش مصرف، بازه‌های ۱۲ و ۲۴ ساعت، تاریخ پنل، محدودسازی لاگ‌ها، هشدار کاربران و آپ‌تایم حفظ شده‌اند.
