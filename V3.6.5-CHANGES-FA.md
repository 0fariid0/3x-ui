# تغییرات Fara Xray v3.6.5

این نسخه مستقیماً روی سورس کامل v3.6.4 ساخته شده است.

## نمودار مصرف قابل تنظیم

- انتخاب بازه‌های ۲۴ ساعت، ۷، ۱۴، ۳۰، ۶۰، ۹۰، ۱۸۰ و ۳۶۵ روز
- نمایش جداگانه مصرف آپلود و دانلود
- نمایش ساعتی آپلود و دانلود در بازه یک‌روزه و نمایش روزانه در بازه‌های بلندتر
- نمایش مصرف کل، میانگین روزانه و تعداد روزهای فعال
- نمایش پرمصرف‌ترین روز، ساعت و دقیقه
- نمایش آخرین دقیقه ثبت‌شده و تعداد دقایق دارای مصرف
- نمایش الگوی مصرف در ۲۴ ساعت شبانه‌روز

## دکمه مستقل گزارش مصرف

در جدول کاربران نسخه دسکتاپ، کنار دکمه اطلاعات کاربر یک دکمه مستقل نمودار اضافه شده است. این دکمه مستقیماً صفحه کامل گزارش مصرف همان کاربر را باز می‌کند.

## هشدار مصرف در نمای کلی

در داشبورد یک بخش جدید برای کاربران پرمصرف و مصرف‌های غیرعادی اضافه شده است. برای هر کاربر موارد زیر نمایش داده می‌شود:

- مصرف کل در بازه انتخاب‌شده
- میانگین مصرف روزانه
- بیشترین مصرف دقیقه‌ای
- تعداد IPهای اخیر
- تعداد هشدارهای ثبت‌شده
- وضعیت عادی، هشدار یا نیازمند بررسی

با انتخاب هر کاربر، گزارش کامل و نمودارهای جزئی او باز می‌شود.

## جزئیات کامل کاربر

صفحه گزارش دارای تب‌های مستقل برای مصرف، IPها، برنامه و سیستم‌عامل، Hostهای استفاده‌شده، هشدارهای مصرف غیرعادی و تاریخچه تغییرات است.

برای مشاهده داده‌های قدیمی‌تر، مقدار «مدت نگهداری گزارش‌ها» در تنظیمات باید برابر یا بیشتر از بازه انتخابی باشد.

## API

Endpoint جدید:

```text
GET /panel/api/clients/usageAlerts?days=7&limit=8
```

Endpoint گزارش کاربر نیز اطلاعات آماری بیشتری مانند مجموع آپلود و دانلود، میانگین روزانه، پرمصرف‌ترین روز، بیشترین مصرف دقیقه‌ای و بازه اولین تا آخرین داده را برمی‌گرداند.
