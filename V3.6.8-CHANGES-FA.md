# تغییرات Fara Xray v3.6.8

## تحلیل مقصدها به‌صورت انتخابی برای هر کاربر

- قابلیت «ثبت مقصدهای اتصال» به‌صورت پیش‌فرض برای همه کاربران خاموش است.
- از صفحه افزودن/ویرایش هر کلاینت می‌توان آن را فقط برای همان کاربر روشن یا خاموش کرد.
- تغییر وضعیت این گزینه، نیاز به راه‌اندازی مجدد Xray را در پنل ثبت می‌کند تا لاگ زمان اجرا فعال یا غیرفعال شود.
- در صفحه جزئیات مصرف، تب «مقصدها» اضافه شده است.
- سرویس‌های شناخته‌شده مانند Instagram، Telegram، YouTube، WhatsApp، TikTok، Discord، Spotify و Netflix دسته‌بندی می‌شوند.
- برای هر مقصد، دامنه یا IP، پورت، پروتکل، تعداد اتصال، اولین و آخرین مشاهده نمایش داده می‌شود.

## حریم خصوصی و مصرف دیسک

- URL کامل، مسیر صفحه، پیام، محتوای ارتباط و DNS payload ذخیره نمی‌شود.
- فقط آمار تجمیعی ساعتی کاربران انتخاب‌شده نگهداری می‌شود.
- نگهداری داده‌های مقصد ۱۴ روز است.
- حداکثر ۱۰۰ مقصد برای هر کاربر در هر ساعت و ۲۰۰ ردیف در هر گزارش نمایش داده می‌شود.
- لاگ اختصاصی خام پس از پردازش دوره‌ای کوتاه می‌شود تا اطلاعات کاربران غیرفعال باقی نماند.
- اگر مدیر قبلاً Access Log اختصاصی خودش را تنظیم کرده باشد، پنل آن فایل را حذف یا کوتاه نمی‌کند.

## محدودیت فنی

- در صورت قابل‌تشخیص‌بودن دامنه توسط Sniffing، نام سرویس دقیق‌تر نمایش داده می‌شود؛ در غیر این صورت فقط IP مقصد ثبت می‌شود.
- این نسخه تعداد اتصال‌ها را تفکیک می‌کند، نه حجم دقیق مصرف هر سرویس.
