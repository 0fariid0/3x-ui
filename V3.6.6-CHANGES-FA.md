# تغییرات Fara Xray v3.6.6

این نسخه مستقیماً روی سورس کامل v3.6.5 ساخته شده است.

## هماهنگی تاریخ و ساعت با پنل

- تمام تاریخ‌ها و ساعت‌های گزارش کاربران، تاریخچه سیستم، لاگ Xray و نمودارهای داشبورد از تقویم انتخاب‌شده پنل استفاده می‌کنند.
- در حالت تقویم شمسی، تاریخ‌ها با تقویم جلالی و اعداد خوانا نمایش داده می‌شوند.
- در حالت میلادی، تمام بخش‌ها به‌صورت یکپارچه از تقویم میلادی استفاده می‌کنند.
- اختلاف زمانی سرور یا حالت تهران از ساعت خود پنل دریافت می‌شود؛ بنابراین منطقه زمانی مرورگر مدیر باعث جابه‌جایی ساعت گزارش‌ها نمی‌شود.
- برچسب روزها و ساعت‌های نمودار نیز با همان تقویم و ساعت پنل ساخته می‌شوند.

## نمودارهای ۱۲ و ۲۴ ساعت گذشته

- بازه‌های جدید «۱۲ ساعت گذشته» و «۲۴ ساعت گذشته» به گزارش مصرف هر کاربر اضافه شدند.
- این دو بازه به‌صورت شناور تا زمان فعلی محاسبه می‌شوند و محدود به ابتدای روز نیستند.
- نمودار، آپلود و دانلود هر ساعت را جداگانه نمایش می‌دهد.
- بازه‌های روزانه ۷، ۱۴، ۳۰، ۶۰، ۹۰، ۱۸۰ و ۳۶۵ روز همچنان در دسترس هستند.
- مجموع مصرف، میانگین، بیشترین ساعت مصرف، تعداد IPها و سایر جزئیات بر اساس همان بازه انتخاب‌شده محاسبه می‌شوند.

## هشدار مصرف مینیمال در نمای کلی

- نمودار میله‌ای مصرف از کارت کاربران پرمصرف حذف شد.
- هر کاربر فقط با اطلاعات ضروری شامل رتبه، نام، مصرف کل، میانگین، تعداد IP، اوج مصرف و وضعیت هشدار نمایش داده می‌شود.
- با انتخاب هر کاربر، گزارش کامل و نمودار جزئی مصرف او باز می‌شود.
- کارت هشدارها در دسکتاپ به‌صورت دو ستونه و کم‌ارتفاع طراحی شده است.

## آپ‌تایم در همان ردیف

- خلاصه آپ‌تایم Xray و سیستم در کنار هشدار مصرف قرار گرفته است.
- طراحی این بخش فشرده شده تا بدون افزایش زیاد ارتفاع داشبورد، وضعیت Xray، سیستم، حافظه و Threadها قابل مشاهده باشد.
- در نمایشگرهای کوچک، دو بخش به‌صورت خودکار زیر یکدیگر قرار می‌گیرند.

## محدودسازی فضای گزارش و لاگ

برای جلوگیری از پرشدن دیسک سرورهای کم‌حجم، داده‌ها چندمرحله‌ای نگهداری می‌شوند:

- نمونه‌های دقیقه‌ای مصرف: حداکثر ۲۵ ساعت
- خلاصه‌های ساعتی: حداکثر ۳۱ روز
- خلاصه‌های روزانه: مطابق مدت نگهداری تنظیم‌شده، با سقف ۳۶۵ روز
- پاک‌سازی داده‌های قدیمی هر ساعت انجام می‌شود.
- دیتابیس SQLite پس از پاک‌سازی دوره‌ای بهینه و WAL آن جمع می‌شود.
- لاگ فعال پنل حداکثر ۳ مگابایت با یک نسخه فشرده پشتیبان است.
- لاگ‌های access و error مربوط به Xray هرکدام حداکثر ۸ مگابایت نگهداری می‌شوند.
- فایل‌های IP Limit هرکدام حداکثر ۴ مگابایت نگهداری می‌شوند.

این محدودیت‌ها فقط سوابق قدیمی را خلاصه یا پاک می‌کنند و مصرف ثبت‌شده کاربران داخل خود Xray را تغییر نمی‌دهند.

## API

Endpoint گزارش کاربر پارامتر زیر را نیز می‌پذیرد:

```text
GET /panel/api/clients/report?email=USER&hours=12
GET /panel/api/clients/report?email=USER&hours=24
```

در بازه ساعتی، خروجی شامل `timelineUsage`، زمان شروع و پایان بازه و مجموع آپلود و دانلود هر ساعت است. OpenAPI، TypeScript، Zod و Schemaهای فرانت‌اند نیز برای نسخه 3.6.6 بازتولید شده‌اند.
