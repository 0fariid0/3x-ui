# تغییرات Fara Xray v3.6.9

## تشخیص دقیق‌تر تلگرام و شبکه Meta

- رنج‌های رسمی IPv4 و IPv6 تلگرام به تشخیص مقصدها اضافه شد.
- آدرس `149.154.167.91` اکنون داخل رنج رسمی `149.154.160.0/20` شناسایی و به‌عنوان Telegram نمایش داده می‌شود.
- رنج‌های شناخته‌شده شبکه Meta به‌صورت داخلی اضافه شدند تا IPهای این شبکه دیگر در گروه «سایر» قرار نگیرند.
- دامنه‌های Instagram همچنان با اطمینان کامل به‌عنوان Instagram نمایش داده می‌شوند.
- IPهای مشترک شبکه Meta با عنوان `Instagram / Meta` نمایش داده می‌شوند؛ زیرا از روی IP مشترک نمی‌توان Instagram را با قطعیت از Facebook یا WhatsApp جدا کرد.

## به‌روزرسانی خودکار رنج‌ها

- فهرست تلگرام هر ۱۲ ساعت از منبع رسمی Telegram به‌روزرسانی می‌شود.
- رنج‌های Meta از Prefixهای جاری AS32934 و AS63293 به‌روزرسانی می‌شوند.
- به‌روزرسانی در پس‌زمینه انجام می‌شود و باعث توقف ثبت مقصدها نمی‌شود.
- اگر سرور به منبع آنلاین دسترسی نداشته باشد، فهرست داخلی همچنان فعال می‌ماند.
- در صورت شکست به‌روزرسانی، تلاش بعدی یک ساعت بعد انجام می‌شود.

## نمایش در گزارش

- نوع اطمینان جدید «شناسایی از رنج شبکه» اضافه شد.
- تشخیص دامنه همچنان اولویت بالاتری از تشخیص IP دارد.
- تنظیم روشن/خاموش‌بودن ثبت مقصدها برای هر کاربر بدون تغییر باقی مانده و به‌صورت پیش‌فرض خاموش است.
