# تغییرات Fara Xray v3.6.11

## تشخیص کامل شبکه Google

- تمام رنج‌های IPv4 و IPv6 منتشرشده در فایل رسمی `goog.json` گوگل داخل سورس قرار گرفتند.
- آی‌پی `142.251.20.113` از رنج `142.250.0.0/15` اکنون با عنوان **Google** و مالک **Google network** نمایش داده می‌شود.
- در تشخیص صرفاً بر اساس IP، عنوان Google به معنی شبکه متعلق به گوگل است و لزوماً سرویس دقیق را مشخص نمی‌کند؛ اگر دامنه قابل تشخیص باشد، سرویس‌هایی مانند YouTube جداگانه نمایش داده می‌شوند.

## به‌روزرسانی خودکار رنج‌ها

- رنج‌های Google هر ۱۲ ساعت از منبع رسمی گوگل به‌روزرسانی می‌شوند.
- رنج‌های Telegram از فهرست رسمی تلگرام و رنج‌های Meta از Prefixهای جاری AS32934 و AS63293 به‌روزرسانی می‌شوند.
- دانلود منابع به‌صورت هم‌زمان و با Timeout مستقل انجام می‌شود؛ خرابی یک منبع مانع به‌روزرسانی بقیه نمی‌شود.
- در صورت شکست یا قطع اینترنت، فهرست کامل داخلی فعال باقی می‌ماند و یک ساعت بعد تلاش مجدد انجام می‌شود.

## اصلاح رکوردهای قبلی

- مقصدهای قبلی که با عنوان `Other` ذخیره شده‌اند، هنگام بازشدن گزارش دوباره طبقه‌بندی می‌شوند.
- فرمت‌های IP شامل فاصله، براکت IPv6 یا `IP:port` نیز قبل از طبقه‌بندی نرمال می‌شوند.
- دکمه پاک‌کردن مقصدهای هر کاربر که در نسخه 3.6.10 اضافه شده بود، حفظ شده است.
