# تغییرات نسخه 3.7.1

این نسخه بر پایه سورس سفارشی 3.6.32 ساخته شده و فقط قابلیت‌ها و اصلاحات منتخب نسخه رسمی 3.7.0 را منتقل می‌کند؛ قابلیت‌های اختصاصی پنل جایگزین یا حذف نشده‌اند.

## مدیریت دوره‌ای کلاینت

- تمدید در روز مشخص تقویمی برای هر کلاینت.
- دوره و روز Reset ترافیک مستقل برای هر کلاینت.
- محدودیت تعداد تمدید خودکار؛ مقدار صفر یعنی بدون محدودیت.
- حفظ رفتار قدیمی تمدید دوره‌ای برای کلاینت‌هایی که تنظیمات جدید را فعال نکرده‌اند.

## محدودیت دستگاه و سابسکریپشن

- تعیین HWID Limit جداگانه برای هر کلاینت.
- مشاهده دستگاه‌های ثبت‌شده.
- حذف یک دستگاه مشخص یا Reset کامل دستگاه‌ها.
- ثبت زمان آخرین Fetch سابسکریپشن برای نمایش و گزارش‌گیری.

## Routing

- انتخاب مستقیم کلاینت در Routing Rule.
- مشاهده، جست‌وجو و انتخاب دسته‌های geosite و geoip.
- دریافت Routing Rule از URL خارجی معتبر و به‌روزرسانی دوره‌ای آن.
- اتصال حساب PIA و Outbound اختصاصی آن عمداً اضافه نشده است.

## پایداری و امنیت

- جلوگیری از حذف اشتباه کلاینت‌ها و اینباندها در Node Sync و نگهداری موقت orphanها تا تأیید حذف واقعی.
- جلوگیری از بازگرداندن تاریخ انقضای قدیمی پس از تمدید و حفظ اینباندهای غیرفعال هنگام Sync.
- اعمال تغییرات runtime مربوط به Bulk و نگهداری ترافیک فقط بعد از Commit موفق دیتابیس.
- اجرای اتمیک Migrationها و جلوگیری از دیتابیس نیمه‌مهاجرت‌کرده.
- حذف IPهای قدیمی از وضعیت کلاینت پس از ویرایش.
- قطع اتصال Hysteria2 پس از عبور از محدودیت.
- حفظ IP Limit وقتی نتیجه بررسی Fail2ban نامشخص یا ناموفق است.
- اعتبارسنجی تمام Certificateهای موجود در bundle مربوط به mTLS.
- ذخیره اتمیک credentialهای mTLS و اعمال Certificate جدید بدون Restart پنل.
- اصلاح لینک‌ها و تنظیمات Hysteria2، Salamander، TLS cipher suites و mport.
- حفظ فایل‌های سفارشی پوشه bin هنگام Update.

## قابلیت‌های اختصاصی حفظ‌شده

- Pin چند کلاینت همراه با رعایت فیلترهای منقضی/غیرفعال/اتمام حجم.
- Route و QR اختصاصی Streisand.
- تشخیص دقیق آنلاین به تفکیک email و inbound و جلوگیری از آنلاین نشان‌دادن همه اینباندها.
- سرعت لحظه‌ای واقعی هر اینباند.
- گزارش مصرف، تاریخچه، مقصدها، برنامه‌ها و IPها.
- تنظیمات اختصاصی سابسکریپشن، WARP و NordVPN.
