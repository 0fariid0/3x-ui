# تغییرات Fara Xray v3.6.13

- افزودن API تاریخچه مصرف Neo Smart برای بازه‌های واقعی ۲۴ ساعت و ۳۰ روز.
- استفاده از همان جدول‌های فشرده موجود پنل؛ بدون ساخت Collector یا جدول تکراری.
- مسیرهای `/sub/:subid/usage`، `/json/:subid/usage` و `/clash/:subid/usage`.
- نگهداری مقصدها فقط تا ۱۵ دقیقه و پاک‌سازی خودکار داده‌های قدیمی هر ۵ دقیقه.
- نمایش مقصدهای فعال/اخیر در بخش جدا با نقطه سبز.
- تازه‌سازی بی‌صدای تب مقصدها هر ۱۵ ثانیه.
- حفظ دکمه پاک‌کردن دستی مقصدهای هر کاربر.
- نمایش نقطه سبز فقط برای مقصدی که در ۵ دقیقه اخیر مشاهده شده است.

> به دلیل محدودیت Access Log، نقطه سبز نشان‌دهنده مشاهده اتصال در پنج دقیقه اخیر است و محتوای ارتباط یا URL کامل ذخیره نمی‌شود.
