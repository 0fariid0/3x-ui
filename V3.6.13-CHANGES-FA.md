# تغییرات Fara Xray v3.6.13

- پشتیبانی Backend موردنیاز قالب **Neo Smart 1.1.0** برای تاریخچه واقعی مصرف سابسکریپشن اضافه شد.
- مسیرهای زیر با استفاده از Rollupهای کم‌حجم موجود پنل فعال شدند:
  - `GET /sub/<sub-id>/usage?range=24h`
  - `GET /sub/<sub-id>/usage?range=30d`
  - همین مسیر برای خروجی‌های JSON و Clash نیز در صورت فعال‌بودن آن‌ها ثبت می‌شود.
- بازه ۲۴ ساعت به‌صورت ۲۴ باکت ساعتی و بازه ۳۰ روز به‌صورت ۳۰ باکت روزانه برگردانده می‌شود.
- جدول و Collector تکراری ساخته نشد؛ API جدید از جدول‌های فشرده فعلی پنل استفاده می‌کند تا مصرف دیسک افزایش پیدا نکند.
- نگهداری مقصدهای اتصال از ۱۴ روز به حالت کوتاه‌مدت تغییر کرد؛ مقصدی که ۱۰ دقیقه فعالیت نداشته باشد خودکار حذف می‌شود.
- مقصدی که در ۲ دقیقه اخیر دیده شده باشد با نقطه سبز و برچسب «فعال» نمایش داده می‌شود.
- مقصدهای فعال در ابتدای فهرست قرار می‌گیرند و تب مقصدها هنگام بازبودن هر ۱۵ ثانیه در پس‌زمینه تازه‌سازی می‌شود.
- دکمه پاک‌کردن دستی مقصدها همچنان حفظ شده است.
