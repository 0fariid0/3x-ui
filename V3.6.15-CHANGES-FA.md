# تغییرات Fara Xray v3.6.15

- رفع خطای Build فرانت‌اند ناشی از ایمپورت اشتباه `react-router-dom`؛ پروژه از بسته نصب‌شده `react-router` استفاده می‌کند.
- حفظ کامل قابلیت‌های نسخه 3.6.14 بدون حذف یا بازگردانی تغییرات.
- تبدیل Release به یک Build پایدار Linux amd64 برای جلوگیری از خطاهای هم‌زمان معماری‌های بلااستفاده.
- اجرای TypeScript type-check، Build نهایی Vite و Build نهایی Go پیش از انتشار.
- بررسی آرشیو و SHA-256 قبل از ساخت GitHub Release.
