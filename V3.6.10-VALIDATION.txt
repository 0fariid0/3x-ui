Fara Xray v3.6.10 validation report
===================================

Implemented
-----------
- Existing destination rows are reclassified with the current domain/CIDR rules when a report is generated.
- Telegram IP-only destinations such as 149.154.167.91 are reported as Telegram even when the database row was previously stored as Other.
- Meta IP-only destinations are reported as Instagram / Meta when they match the bundled or refreshed Meta prefixes.
- Destination upserts refresh service, owner, domain/IP, protocol, port, and confidence fields.
- Added a per-client Clear destination history action in the Destinations tab.
- Added POST /panel/api/clients/clearDestinations/:email.
- Pending complete access-log lines are ingested before deletion so pre-reset rows do not reappear on the next collector run.

Validation completed
--------------------
- Parsed 492 Go source files successfully with go/parser.
- gofmt applied to modified Go files.
- Telegram test address 149.154.167.91 matched 149.154.160.0/20 in an isolated Go test.
- Meta test address 157.240.10.35 matched the bundled Meta range in an isolated Go test.
- TypeScript/TSX syntax check completed; only expected missing-module diagnostics occurred because node_modules is not bundled.
- English and Persian translation JSON files parse successfully.
- Frontend and docs OpenAPI JSON parse successfully and include the destination reset endpoint.
- install.sh, update.sh, and x-ui.sh pass bash -n.
- CSS brace check passed.
- Whitespace check passed.

Environment limitation
----------------------
The local environment provides Go 1.23 and Node 22, while this project requires Go 1.26.5 and Node 24. A full dependency-aware Go build and production Vite build could not be completed locally. GitHub Actions remains the final full-toolchain build check after pushing tag v3.6.10.
