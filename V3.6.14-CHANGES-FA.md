# تغییرات Fara Xray v3.6.14

- خطای مشترک همه Buildهای Release برطرف شد.
- `actions/checkout@v7` به نسخه معتبر `actions/checkout@v6` تغییر کرد.
- `actions/setup-go@v7` به نسخه معتبر `actions/setup-go@v6` تغییر کرد.
- `actions/setup-node@v7` به نسخه معتبر `actions/setup-node@v6` تغییر کرد.
- نسخه‌های معتبر `actions/upload-artifact@v7` و `actions/download-artifact@v8` حفظ شدند.
- تمام قابلیت‌های نسخه 3.6.13، شامل آمار Neo Smart و مقصدهای فعال، بدون تغییر باقی مانده‌اند.
