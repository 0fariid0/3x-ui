# تغییرات Fara Xray v3.6.29

## بکاپ استاندارد و قابل انتقال

در نسخه 3.6.29 بکاپ SQLite پنل از روی یک Snapshot موقت ساخته می‌شود. جدول‌های آماری و تاریخچه‌ای که مخصوص فورک Fara Xray هستند فقط از فایل موقت بکاپ حذف می‌شوند و دیتابیس اصلی سرور هیچ تغییری نمی‌کند.

اطلاعات اصلی و استاندارد مطابق سورس MHSanaei/3x-ui حفظ می‌شوند؛ شامل کاربران، این‌باندها، کلاینت‌ها، تنظیمات، ترافیک اصلی، نودها، Hostها، گروه‌ها، توکن‌ها، fallbackها و subscriptionهای اصلی.

بعد از حذف جدول‌های غیرضروری از Snapshot، روی همان فایل موقت VACUUM و integrity_check اجرا می‌شود تا هم حجم واقعی فایل کاهش پیدا کند و هم فایل خراب تحویل داده نشود.

Restore/Import اصلی پنل دست‌کاری نشده است. پس از Import، migrationهای خود فورک جدول‌های آماری سفارشی را دوباره به‌صورت خالی ایجاد می‌کنند.

دیتابیس زنده، تاریخچه‌ها و آمار فعلی سرور هنگام گرفتن بکاپ حذف یا پاک نمی‌شوند.
